{
  "name": "improved-funicular-soroban",
  "version": "1.0.0",
  "description": "Place-Value Peg Soroban — a modular, SOLID, testable ESM refactor",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8139 --protocol HTTP/1.1",
    "test": "node --test",
    "test:e2e": "playwright test",
    "offline:manifest": "node scripts/offline-manifest.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
