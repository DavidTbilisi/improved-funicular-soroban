# Local build/tooling artifacts — the place file is built with `rojo build`,
# and the rojo binary is fetched per-machine (see README).
*.rbxlx
*.rbxlx.lock
*.rbxl
rojo.exe
rojo-*.zip
luau*.exe
luau-*.zip
# rendered by scripts/make-sounds.mjs; uploaded to Roblox once, not versioned
sounds/
